1. Страница работы с партнерами (интерфейс директора) WorkWithPartnerBase: доделать изменение данных о партнере в БД. В чем проблема если надо изменить запись - мы ее выделяем меняем значения в полях,
нажимаем "Изменить", если выбраны данные в том числе и о городах присутствия и предлагаемых продуктах, то все проходит гладко (формируется новый объект Partner - запись таблицы, она добавляется
в БД, старая запись удаляется). Если же инфу о городах присутствия и предлагаемых продуктах не выбрать, то по задумке должен быть ссответствующий запрос об использовании старых данных
(это работает) и в новый объект должны копироваться данные из старого. Здесь что-то не так: вроде как изменения сохраняются, но данные о городах и продуктах пустые.

2. В ListView добавить границы-разделители между столбцами

3. При загрузке фото открывается окно выбор файла. Если в момент выбора файла он находится "над" областью Canvas, то срабатывает событие MouseMove и в этом случае на загруженной картинке есть прямоугольник выделения

4. Не сделана печать документов

5. На 3 этапе работы с программой не отображаются данные на StackPanel

6. Подключить ФИАС и сделать строку быстрого поиска  